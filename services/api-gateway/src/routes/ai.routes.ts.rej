diff a/services/api-gateway/src/routes/ai.routes.ts b/services/api-gateway/src/routes/ai.routes.ts	(rejected hunks)
@@ -31,7 +31,8 @@ router.post('/chat',
         throw new AppError('Invalid input', 400);
       }
 
-      const userId = (req as any).user.userId;
+      const userId = req.user?.userId;
+      if (!userId) throw new AppError('Unauthorized', 401);
       const { message, context } = req.body;
       const response = await aiService.chat(userId, message, context);
       res.json({ response });
@@ -73,7 +74,8 @@ router.post('/generate-roadmap',
         throw new AppError('Invalid input', 400);
       }
 
-      const userId = (req as any).user.userId;
+      const userId = req.user?.userId;
+      if (!userId) throw new AppError('Unauthorized', 401);
       const roadmap = await aiService.generateRoadmap(userId, req.body);
       res.json({ roadmap });
     } catch (error) {
@@ -135,7 +137,8 @@ router.post('/feedback',
         throw new AppError('Invalid input', 400);
       }
 
-      const userId = (req as any).user.userId;
+      const userId = req.user?.userId;
+      if (!userId) throw new AppError('Unauthorized', 401);
       const { responses, jobType } = req.body;
       const feedback = await aiService.getInterviewFeedback(userId, responses, jobType);
       res.json({ feedback });
